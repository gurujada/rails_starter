<section class="section">
  <div class="section-body">
    <div class="card">
      <div class="card-body">
        <%%= render 'form', <%= singular_table_name %>: @<%= singular_table_name %> %>
      </div>
    </div>
  </div>
</section>

<%%= link_to icon('fas fa-eye', 'Show'), @<%= singular_table_name %>, data: { turbolinks_action: 'replace' }, class: 'btn btn-icon icon-left btn-info' %>

<%% content_for(:page_title, 'Editing <%= singular_table_name.titleize %>') %>